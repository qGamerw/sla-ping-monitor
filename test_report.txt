## 📌Общая информация

- **Проект**: SLA Ping Monitor — сервис мониторинга доступности HTTP-эндпоинтов и сбора SLA-метрик.
- **Тип архитектуры**: клиент-серверное приложение с REST API.
- **Под архитектура**: монолит с слоистой структурой (API → business/service → persistence).
- [Ссылка на git](/workspace/sla-ping-monitor)

#### Технологический стек:
- **Язык программирования**: Kotlin (JVM 21).
- **Фреймворк**: Spring Boot 3.5 (Web, Data JPA, Actuator, Validation).
- **Сборщик проекта**: Gradle Kotlin DSL.
- **База данных**: PostgreSQL (runtime), H2 (тесты).
- **API**: REST (JSON), Spring MVC controllers.
- **Хранилище файлов**: не используется (данные хранятся в БД).
---
## 🎯Функциональные требования
- Управление эндпоинтами мониторинга (CRUD): создание, обновление, удаление, получение списка и деталей.
- Конфигурация проверки для эндпоинта: URL, метод, заголовки, таймауты, ожидаемые статус-коды, интервал, теги, включение/выключение.
- Группировка эндпоинтов в папки: создание, обновление, удаление, получение списка.
- Планирование и выполнение периодических проверок, фиксация успешных и ошибочных результатов.
- Сохранение результатов проверок с метриками (latency, статус, тип/сообщение ошибки).
- Агрегация статистики по окну времени: p50/p95/p99, среднее, минимум/максимум, errorRate, lastStatus, insufficientSamples.
- Получение резюме по всем эндпоинтам, включая данные последней проверки и статистику окна.
- Регистрация и мониторинг backend-ноды (heartbeat/метаданные).
- Доступность технических эндпоинтов health/metrics через Spring Boot Actuator.

---
## 🗃Структура базы данных
- **endpoints**
  - `id` (UUID, PK)
  - `name`, `url`, `method`
  - `headers` (JSON), `expected_status` (JSON)
  - `timeout_ms`, `interval_sec`, `enabled`
  - `tags` (JSON), `lease_owner`, `lease_until`
  - `created_at`, `updated_at`, `version`, `is_deleted`
- **check_results**
  - `id` (UUID, PK)
  - `endpoint_id` (FK → endpoints.id)
  - `started_at`, `finished_at`
  - `latency_ms`, `status_code`, `success`
  - `error_type`, `error_message`
- **folders**
  - `id` (UUID, PK)
  - `name` (unique)
  - `endpoints` (список UUID)
- **backend_nodes**
  - `node_id` (PK)
  - `base_url`
  - `started_at`, `last_heartbeat_at`
  - `meta` (JSON)

---
## 🔌 Основные эндпоинты
- **Эндпоинты мониторинга**
  - `POST /api/endpoints` — создать эндпоинт.
  - `PUT /api/endpoints/{id}` — обновить эндпоинт.
  - `DELETE /api/endpoints/{id}` — удалить эндпоинт.
  - `GET /api/endpoints/{id}` — получить эндпоинт.
  - `GET /api/endpoints` — список эндпоинтов.
- **Статистика и проверки**
  - `GET /api/endpoints/{id}/stats?windowSec=...` — статистика по окну.
  - `GET /api/endpoints/{id}/checks?from=...&to=...` — список проверок.
  - `GET /api/endpoints/summary?windowSec=...` — краткое резюме по всем эндпоинтам.
- **Папки**
  - `POST /api/folders` — создать папку.
  - `PUT /api/folders` — обновить папку.
  - `DELETE /api/folders` — удалить папку.
  - `GET /api/folders` — список папок.
- **Backend-ноды**
  - `GET /api/nodes` — данные backend-ноды.
- **Actuator**
  - `GET /actuator/health`
  - `GET /actuator/metrics`

---
## 📊Аналитика и отчётность
- Агрегированная статистика по окну времени: sampleCount, p50/p95/p99, avg, min/max, errorRate, lastStatus, insufficientSamples.
- Резюме по всем эндпоинтам с последней проверкой и статистикой окна.
- Возможность выгрузки детальных проверок за период (from/to).

---
## 📄Документация
- README.md с описанием проекта, стека, требований и базовых эндпоинтов.
- Примеры запросов в `apps/backend/src/main/resources/local.http`.
- Настройки окружения в `apps/backend/src/main/resources/application.yml`.

## Архитектуру: **Компоненты** и **Взаимодействие**
- **Компоненты**:
  - **API-слой (контроллеры)**: принимает REST-запросы и валидирует входные данные.
  - **Business/service-слой**: реализует бизнес-логику, расчёт статистики и работу с планировщиком.
  - **Scheduler/processor**: периодически инициирует проверки эндпоинтов и пишет результаты.
  - **Persistence-слой**: JPA-сущности и репозитории для доступа к данным в PostgreSQL.
  - **HTTP-клиент**: выполняет запросы к проверяемым endpoints.
  - **Actuator**: предоставляет health/metrics для мониторинга.
- **Взаимодействие**:
  - Клиенты вызывают REST API → контроллеры → сервисы → репозитории → БД.
  - Планировщик получает список активных endpoints → HTTP-клиент выполняет проверки → результаты сохраняются в БД.
  - Сервис статистики читает результаты проверок → отдаёт агрегаты через API.
