apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: sla-backend
spec:
  selector:
    app: sla-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
